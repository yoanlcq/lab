[package]
name = "tracing_facade"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tracy-client = { version = "0.18.2", optional = true }
tunables = { path = "../../application/tunables" }
linkme = "0.3"

[lints]
workspace = true

[features]
default = ["tracy"]
tracy = ["dep:tracy-client"]
