[package]
name = "huge"
version = "0.1.0"
edition = "2024"
publish = false

[lints]
workspace = true

[features]
default = ["track_allocation_sizes"]
track_allocation_sizes = []

[dependencies]
parking_lot = "0.12.4"
virtual_memory = { path = "../virtual_memory" }
unique_ptr = { path = "../unique_ptr" }
